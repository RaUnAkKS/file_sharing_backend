# from django.urls import path
# from .views import *

# urlpatterns = [
#     path('create/', CreateShareLinkView.as_view(), name='create-share-link'),
#     path('<uuid:id>/', ShareLinkDetailView.as_view(), name='share-link-detail'),
#     path('access/<uuid:id>/', ShareLinkAccessView.as_view(), name='share-link-access'),
# ]
